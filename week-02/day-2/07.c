#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main()
{
	int h1 = 350;
	int h2 = 200;
	// tell if the double of h2 is bigger than h1
	if(2*h2>h1){
        printf("double of h2: %d is bigger than h1: %d",h2*2,h1);
	}
	return 0;
}
